{
  "schema_version": 1,
  "note": "These paths are owned by the assignment, not the student. Tampering with them is detected on push (server-side via autograde workflow) and on `gh student submit` (client-side). See roadmap design question #6.",
  "paths": [
    ".classroom/",
    ".github/",
    "tests/",
    "Makefile"
  ],
  "enforcement": {
    "client_side": "warn_and_block",
    "server_side": "fail_autograde_with_tamper_status"
  }
}
